import { View, Text, Modal } from 'react-native'
import React from 'react'
import tw from 'twrnc'

export default function EditUser({ isVisible }: any) {
    return (
        <Modal
            visible={isVisible}
            transparent={true}
            animationType="fade"
        >

        </Modal>
    )
}